import React from 'react'

const Section = ({ children }) => {
    return (
        <section>
            <div className='root-section '>{children}</div>
        </section>
    )
}

export default Section
